# ================================================================================
# VARIÁVEIS DE AMBIENTE - DoeCerto API
# ================================================================================
# 
# ⚠️  NUNCA commite o arquivo .env com credenciais reais!
# 
# SETUP LOCAL:
#   cp .env.example .env && nano .env
# 
# PRODUÇÃO:
#   Configure as variáveis no seu serviço de hosting (Railway, Render, etc.)
#   ou use Docker secrets / GitHub Secrets para valores sensíveis
# 
# ================================================================================

# ========================================
# 🔴 OBRIGATÓRIAS - Defina ANTES de iniciar
# ========================================

# Database - MySQL/MariaDB
# Formato: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# ⚠️  Use senha forte em produção (mínimo 16 caracteres, alfanuméricos + símbolos)
DATABASE_URL=

# JWT Secret - Chave para assinar tokens de autenticação
# ⚠️  OBRIGATÓRIO: Gere uma chave única e segura!
#     Comando: openssl rand -base64 32
#     Nunca reutilize entre ambientes (dev/staging/prod)
JWT_SECRET=

# Cache - Redis
# Formato: redis://[:PASSWORD@]HOST:PORT
REDIS_URL=

# ========================================
# 🟡 RECOMENDADAS PARA PRODUÇÃO
# ========================================

# URL pública da API (usada em links de email, webhooks)
# Exemplo: https://api.doecerto.com.br
APP_URL=

# CORS - URL do frontend (aceita requisições apenas desta origem)
# Exemplo: https://doecerto.com.br
FRONTEND_URL=

# ========================================
# 📧 EMAIL/SMTP (opcional - se não definir, apenas loga no console)
# ========================================

# Gmail: Ative 2FA e crie App Password em https://myaccount.google.com/apppasswords
# Outros: Use credenciais do seu provedor SMTP
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=

# ========================================
# ⚙️  CONFIGURAÇÕES OPCIONAIS
# ========================================

# Porta do servidor (padrão: 3000)
# PORT=3000

# JWT - Tempo de expiração (padrão: 1h)
# JWT_EXPIRES_IN=1h

# ========================================
# 🧪 APENAS DESENVOLVIMENTO
# ========================================

# Executar seed no startup do Docker (NUNCA true em produção!)
# RUN_SEED=false